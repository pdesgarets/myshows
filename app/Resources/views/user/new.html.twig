{% extends 'base.html.twig' %}

{% block body %}
    <h1>User creation</h1><p class="pull-right"><a href="{{ path('user_index') }}">Back to the list</a></p>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-default" value="Create" />
    {{ form_end(form) }}
{% endblock %}
